<% if (success.length > 0) { %>
  <div class="alert alert-success"><%= success %></div>
<% } %>

<div class="jumbotron text-center">
  <h2><%= event.name %></h2>
  <p><%= event.description %></p>
</div>
